# WinGet manifests

Versioned WinGet manifests are generated here by the private development repository's release process. Validate a generated version directory with `winget validate <directory>` before submitting it to `microsoft/winget-pkgs`.
